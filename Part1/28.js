// sets

// sets also have itd own method

// no index based access
//order is not guaranteed
//uniqe item only (no duplicate allow)

// const numbers = new Set([1, 2, 3]);

const numbers = new Set();

numbers.add(1);
numbers.add("item1");
console.log(numbers);